## 1. Lifecycle orchestration
- [x] 1.1 Implement build, update, drop, info, and check/status operations in the shared core lifecycle service.
- [x] 1.2 Add locking and background job orchestration.
- [x] 1.3 Define consistent unsupported-capability behavior for adapters that cannot perform a requested lifecycle action.

## 2. Acceptance
- [x] 2.1 Add tests for path-only lifecycle flows.
- [x] 2.2 Add tests for lifecycle locking and status reporting under concurrent requests.
