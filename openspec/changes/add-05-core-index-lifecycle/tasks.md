## 1. Lifecycle orchestration
- [ ] 1.1 Implement build, update, drop, info, and check/status operations in the shared core lifecycle service.
- [ ] 1.2 Add locking and background job orchestration.
- [ ] 1.3 Define consistent unsupported-capability behavior for adapters that cannot perform a requested lifecycle action.

## 2. Acceptance
- [ ] 2.1 Add tests for path-only lifecycle flows.
- [ ] 2.2 Add tests for lifecycle locking and status reporting under concurrent requests.
