## 1. Project context
- [x] 1.1 Review the current repository sources of truth (`README.md`, `docs/ARCHITECTURE.md`, `pyproject.toml`, and repo instructions).
- [x] 1.2 Replace the template content in `openspec/project.md` with concrete project context and conventions.

## 2. Validation
- [x] 2.1 Validate the tooling-only change with `openspec validate document-project-context --strict --no-interactive`.
- [x] 2.2 Confirm `openspec/project.md` no longer contains placeholder template text.
