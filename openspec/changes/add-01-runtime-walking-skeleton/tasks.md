## 1. Runtime skeleton
- [x] 1.1 Create the minimal runtime entrypoints for `start`, `execute`, and `end`.
- [x] 1.2 Implement session tracking and lifecycle ownership for a single runtime process.
- [x] 1.3 Implement a persistent sandbox namespace that survives multiple execute calls within a session.

## 2. Acceptance
- [x] 2.1 Demonstrate direct-path repository exploration without registry or adapter-specific features.
- [x] 2.2 Add tests for session lifecycle and namespace persistence.
